node_modules/
dist/
frontend/dist/
backend/dist/
*.log
.DS_Store
.vite/
coverage/
scratch/

# Substrait link (machine-local, gitignored by design)
.substrait/

# NOTE: frontend/public/assets/** is generated by `npm run gen:assets` but is
# committed on purpose — the connect-mode deploy builds the pushed branch and its
# frontend Docker image does not run codegen. Regenerate + commit when sprites change.
