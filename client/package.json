{
  "name": "@mugen/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "@mugen/shared": "*"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  }
}
